# TensorRT Diffusion Model Optimization Pipeline
# Core dependencies for NVIDIA T4 GPU optimization on Google Colab
#
# NOTE: On Google Colab, torch and torchvision are pre-installed.
# Do NOT reinstall them to avoid version conflicts.
# Install with: pip install -e . --no-deps && pip install diffusers transformers accelerate

# TensorRT integration (install separately on Colab if needed)
# torch-tensorrt>=2.1.0
# tensorrt>=8.6

# NVIDIA Model Optimizer for INT8 PTQ
nvidia-modelopt>=0.15.0

# Diffusion model support
diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0

# Image processing
Pillow>=9.0.0

# Numerical computing
numpy>=1.24.0

# Development dependencies
pytest>=7.0
hypothesis>=6.0
pytest-benchmark>=4.0
