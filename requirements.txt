# TensorRT Diffusion Model Optimization Pipeline
# Core dependencies for NVIDIA T4 GPU optimization on Google Colab
#
# NOTE: On Google Colab (CUDA 13.x), use the CUDA 13 specific packages:
#   pip install tensorrt-cu13 tensorrt-lean-cu13 nvidia-modelopt>=0.39
#
# For local development with CUDA 12.x:
#   pip install tensorrt torch-tensorrt nvidia-modelopt>=0.15.0

# TensorRT for CUDA 13 (Google Colab March 2026+)
# These are installed explicitly in the notebook
# tensorrt-cu13
# tensorrt-lean-cu13

# NVIDIA Model Optimizer for INT8 PTQ (v0.39+ for CUDA 13)
nvidia-modelopt>=0.39.0

# Diffusion model support
diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0

# Image processing
Pillow>=9.0.0

# Numerical computing
numpy>=1.24.0

# Development dependencies
pytest>=7.0
hypothesis>=6.0
pytest-benchmark>=4.0
